/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'avataaars.io', 
        'admin.wellness4u.in'
      ],
    },

    // middleware: {
    //   '/account': true,  // Apply middleware to /account route

 
    // },


  };
  
  export default nextConfig;
  